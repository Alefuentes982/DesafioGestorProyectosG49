<div class="container mt-5">
    <% if @project.errors.any? %>
        <div id="error_explanation">
            <div class="alert alert-danger" role="alert">
                <h4 class="alert-heading"><strong>Parece que...</strong> ...algo anda mal</h4>
                <p><%= pluralize(@project.errors.count, "error") %> prohibited this project from being created:</p>
                <hr>
                <ul>
                    <% @project.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                </ul>
            </div>
        </div>
    <% else %>
        <div class="alert alert-success text-center" role="alert">
            <h4 class="alert-heading">Proyecto creado exitosamente !!!</h4>
        </div>    
    <% end %>
    <%= link_to "Back to Dashboard", root_path %>
</div>

